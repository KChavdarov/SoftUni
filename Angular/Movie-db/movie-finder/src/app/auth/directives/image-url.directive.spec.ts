import { ImageUrlDirective } from './image-url.directive';

describe('ImageUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageUrlDirective();
    expect(directive).toBeTruthy();
  });
});
