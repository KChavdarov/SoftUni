import { IsImageDirective } from './is-image.directive';

describe('IsImageDirective', () => {
  it('should create an instance', () => {
    const directive = new IsImageDirective();
    expect(directive).toBeTruthy();
  });
});
