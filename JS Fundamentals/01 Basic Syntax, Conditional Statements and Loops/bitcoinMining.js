function bitcoinMining(input) {
    let index = 0;
    let shift = input[index];
    index++;
    let totalGold = 0;
    let days = 0;

    for(let i = 0; i < input.length; i++);
        days++;
        if (days %3 ==0){
            
        }



}